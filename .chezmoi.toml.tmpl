[data]
    name = "{{ promptStringOnce . "name" "Full name" "louis" }}"
    email = "{{ promptStringOnce . "email" "Git email" "louis@timetreeapp.com" }}"
    signingkey = "{{ promptStringOnce . "signingkey" "GPG signing key (leave empty to disable)" "" }}"
